<UserControl x:Class="SpectrumAnalysis.Module.View.Menu.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Menu>
        <MenuItem Header="Menu">
            <MenuItem Header="New" Command="{Binding New}" />
            <MenuItem Header="Open" Command="{Binding Open}" />
            <MenuItem Header="Clean Data" Command="{Binding CleanData}" />
            <MenuItem Header="Clean Analysis" Command="{Binding CleanAnalysis}" />
            <MenuItem Header="Exit" Command="{Binding Exit}" />
        </MenuItem>
        <MenuItem Header="Automate" Command="{Binding Run}" />
        <MenuItem Header="About" Command="{Binding About}" />
    </Menu>
</UserControl>